[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cat-llm"
dynamic = ["version"]
description = "The CatLLM ecosystem — LLM-powered classification for text, images, and PDFs across domains."
readme = "README.md"
requires-python = ">=3.9"
license = "GPL-3.0-or-later"
keywords = ["llm", "classification", "text analysis", "survey", "social media", "academic", "cognitive assessment"]
authors = [
  { name = "Chris Soria", email = "chrissoria@berkeley.edu" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
]
dependencies = [
  "cat-stack>=2.4.0",
  "cat-survey>=0.2.3",
  "cat-vader>=1.13.5",
  "cat-ademic>=0.2.2",
  "cat-cog>=0.2.1",
  "cat-pol>=1.3.3",
  "cat-web>=0.2.3",
  # [claude] is required: cat-claws 0.3.0 split its SDKs into extras, and a
  # bare pin would silently drop claude-agent-sdk from every cat-llm install.
  "cat-claws[claude]>=0.3.0",
]

[project.optional-dependencies]
pdf = ["cat-stack[pdf]"]
embeddings = ["cat-stack[embeddings]"]
formatter = ["cat-stack[formatter]"]
# ChatGPT-subscription backend (model_source="codex-agent"). Optional: the
# openai-codex SDK ships platform wheels with a bundled codex binary — too
# heavy to force on every cat-llm user.
codex = ["cat-claws[codex]>=0.3.0"]

[project.urls]
Documentation = "https://github.com/chrissoria/cat-llm#readme"
Issues = "https://github.com/chrissoria/cat-llm/issues"
Source = "https://github.com/chrissoria/cat-llm"

[tool.hatch.version]
path = "src/catllm/__about__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/catllm", "src/cat_llm"]

[tool.hatch.build.targets.sdist]
include = [
    "src/catllm/**/*.py",
    "src/cat_llm/__init__.py",
]
