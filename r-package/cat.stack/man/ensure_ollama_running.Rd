% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama.R
\name{ensure_ollama_running}
\alias{ensure_ollama_running}
\title{Ensure a local Ollama server is running}
\usage{
ensure_ollama_running(
  auto_start = TRUE,
  timeout = 30,
  host = "localhost",
  port = 11434L,
  verbose = TRUE
)
}
\arguments{
\item{auto_start}{Logical. If \code{TRUE} (default), attempt to launch
Ollama when not running. If \code{FALSE}, just check and error if not
running.}

\item{timeout}{Numeric. Seconds to wait for Ollama to become ready
after \code{auto_start}. Default \code{30}.}

\item{host}{Character. Hostname Ollama is reachable on.
Default \code{"localhost"}.}

\item{port}{Integer. Port Ollama is reachable on. Default \code{11434L}.}

\item{verbose}{Logical. Print status messages. Default \code{TRUE}.}
}
\value{
Invisibly returns \code{TRUE} when Ollama is running.
}
\description{
Checks whether an Ollama server is reachable at \code{host:port}. If not,
attempts to start it using the platform-appropriate command and
polls until the server responds (or \code{timeout} is reached). Call
this once at the top of an R session before classifying with
\code{model_source = "ollama"}.
}
\details{
Platform start commands:
\itemize{
\item \strong{macOS} — \verb{open -a Ollama} (launches the Ollama.app daemon).
Falls back to \verb{ollama serve} if the app is not installed.
\item \strong{Linux} — \verb{ollama serve} (run in a detached process).
\item \strong{Windows} — \verb{ollama serve}.
}

If Ollama is not installed, the function returns a clear error
message linking to \url{https://ollama.com}.
}
\examples{
\dontrun{
# Ensure Ollama is up before classifying with a local model
ensure_ollama_running()

results <- classify(
  input_data   = c("text 1", "text 2"),
  categories   = c("Positive", "Negative", "Neutral"),
  user_model   = "qwen2.5:7b",
  model_source = "ollama"
)

# Just check without auto-starting
ensure_ollama_running(auto_start = FALSE)
}
}
