% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.validate_add_other}
\alias{.validate_add_other}
\title{Validate and normalise the \code{add_other} argument}
\usage{
.validate_add_other(add_other)
}
\arguments{
\item{add_other}{\code{FALSE}, \code{TRUE}, or \code{"prompt"}.}
}
\value{
The validated value (unchanged if already valid).
}
\description{
Python's default for \code{add_other} is \code{"prompt"}, which calls \code{input()}.
In non-interactive sessions, Python's \code{input()} raises \code{EOFError}, which
the Python code catches and treats as "no". The R default matches Python's
default (\code{"prompt"}). This helper validates the user's input.
}
\keyword{internal}
