% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompt_tune.R
\name{prompt_tune}
\alias{prompt_tune}
\title{Optimize a classification prompt with human-in-the-loop feedback}
\usage{
prompt_tune(
  input_data,
  categories,
  api_key = NULL,
  user_model = "gpt-4o",
  model_source = "auto",
  models = NULL,
  description = "",
  survey_question = "",
  sample_size = 10L,
  max_iterations = 3L,
  multi_label = TRUE,
  creativity = NULL,
  use_json_schema = TRUE,
  consensus_threshold = "unanimous",
  max_retries = 5L,
  input_mode = NULL,
  ui = "terminal",
  optimize = "balanced",
  add_other = "prompt",
  thinking_budget = 0L,
  auto_start_ollama = TRUE
)
}
\arguments{
\item{input_data}{A character vector, list, or \code{data.frame} column
of items to classify during tuning.}

\item{categories}{A character vector of category names. The labels
themselves are never modified by tuning — only the classification
instructions change.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{user_model}{Character. Model name. Default \code{"gpt-4o"}.}

\item{model_source}{Character. Provider hint. Default \code{"auto"}.}

\item{models}{List of model specs for ensemble mode (each
\code{c(model, provider, api_key)}). Overrides \code{user_model}/\code{api_key}/
\code{model_source} if given. Default \code{NULL}.}

\item{description}{Character. Context description. Default \code{""}.}

\item{survey_question}{Character. Survey question for context.
Default \code{""}.}

\item{sample_size}{Integer. Items to test per iteration. Default \code{10L}.}

\item{max_iterations}{Integer. Max instruction attempts per
category. Default \code{3L}.}

\item{multi_label}{Logical. Multi-label classification. Default \code{TRUE}.}

\item{creativity}{Numeric or \code{NULL}. Temperature. Default \code{NULL}.}

\item{use_json_schema}{Logical. Default \code{TRUE}.}

\item{consensus_threshold}{Character or numeric. For ensemble mode.
Default \code{"unanimous"}.}

\item{max_retries}{Integer. Default \code{5L}.}

\item{input_mode}{Character or \code{NULL}. Input mode override.}

\item{ui}{Character. Review interface for corrections.
\code{"terminal"} (default in R) reads from stdin. \code{"browser"} opens a
local web page with checkboxes (may not auto-launch from R sessions).}

\item{optimize}{Character. Which metric to maximize.
\code{"balanced"} (default), \code{"precision"}, or \code{"sensitivity"}.}

\item{add_other}{Logical or \code{"prompt"}. Controls auto-addition of an
"Other" catch-all category. Default \code{"prompt"}.}

\item{thinking_budget}{Integer. Default \code{0L}.}

\item{auto_start_ollama}{Logical. If \code{TRUE} (default), automatically
call \code{\link[=ensure_ollama_running]{ensure_ollama_running()}} when \code{model_source = "ollama"} or
any ensemble entry uses the \code{"ollama"} provider. Set \code{FALSE} to
skip the check.}
}
\value{
A named list with components:
\itemize{
\item \code{system_prompt} — the optimized system prompt (best found)
\item \code{iterations} — list of per-iteration records (label,
system_prompt, metrics, per_category, total_flips)
\item \code{per_category_summary} — per-category metrics from the
best-scoring iteration
}
}
\description{
Wraps the Python \code{catstack.prompt_tune()} function. Runs a
coordinate-descent loop: classifies a small sample, asks you to
correct the model's output, then has a meta-LLM rewrite the
classification instructions for each category that had errors.
Returns the best system prompt found plus per-iteration metrics.
}
\details{
This function is \strong{interactive} — you'll be asked to review and
correct the model's labels at least once. From an R session, the
default \code{ui = "terminal"} reads your corrections from stdin (works
in R, Rscript, and most IDE consoles). \code{ui = "browser"} opens a
local web page with checkboxes; depending on your R setup this may
or may not auto-launch the browser, so terminal is the safer
default for R users.

Use the returned \code{system_prompt} with \code{\link[=classify]{classify()}} via the
\verb{system_prompt =} argument to apply the tuned instructions.
}
\examples{
\dontrun{
result <- prompt_tune(
  input_data    = df$open_response,
  categories    = c("Positive", "Negative", "Neutral"),
  api_key       = Sys.getenv("OPENAI_API_KEY"),
  user_model    = "gpt-4o-mini",
  sample_size   = 10L,
  max_iterations = 3L,
  ui            = "terminal"
)

# Inspect the optimized prompt
cat(result$system_prompt)

# Use it in classify() via the system_prompt argument
results <- classify(
  input_data    = df$open_response,
  categories    = c("Positive", "Negative", "Neutral"),
  api_key       = Sys.getenv("OPENAI_API_KEY"),
  user_model    = "gpt-4o-mini",
  system_prompt = result$system_prompt
)
}
}
