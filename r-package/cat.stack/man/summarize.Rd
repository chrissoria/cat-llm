% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize}
\alias{summarize}
\title{Summarize text, images, or PDFs using LLMs}
\usage{
summarize(
  input_data,
  api_key = NULL,
  description = "",
  instructions = "",
  format = "paragraph",
  max_length = NULL,
  focus = NULL,
  user_model = "gpt-4o",
  model_source = "auto",
  mode = "image",
  input_mode = NULL,
  input_type = "auto",
  pdf_dpi = 150L,
  creativity = NULL,
  thinking_budget = 0L,
  chain_of_thought = TRUE,
  context_prompt = FALSE,
  step_back_prompt = FALSE,
  filename = NULL,
  save_directory = NULL,
  models = NULL,
  max_workers = NULL,
  parallel = NULL,
  auto_download = FALSE,
  safety = FALSE,
  max_retries = 5L,
  batch_retries = 1L,
  retry_delay = 1,
  row_delay = 0,
  fail_strategy = "partial",
  batch_mode = FALSE,
  batch_poll_interval = 30,
  batch_timeout = 86400,
  auto_start_ollama = TRUE
)
}
\arguments{
\item{input_data}{A character vector, list, or \code{data.frame} column. For
images/PDFs, a directory path or character vector of file paths.}

\item{api_key}{Character or \code{NULL}. API key for the model provider
(single-model mode). Not required when \code{models} is supplied. Default
\code{NULL}.}

\item{description}{Character. Context description for the summarization
task. Default \code{""}.}

\item{instructions}{Character. Specific instructions for the summary.
Default \code{""}.}

\item{format}{Character. Output format: \code{"paragraph"} (default) or other
supported formats.}

\item{max_length}{Integer or \code{NULL}. Maximum length of the summary. \code{NULL}
uses the model default. Default \code{NULL}.}

\item{focus}{Character or \code{NULL}. Optional focus for the summary. Default
\code{NULL}.}

\item{user_model}{Character. Model name. Default \code{"gpt-4o"}.}

\item{model_source}{Character. Provider hint: \code{"auto"}, \code{"openai"},
\code{"anthropic"}, \code{"google"}, etc. Default \code{"auto"}.}

\item{mode}{Character. Processing mode for images/PDFs: \code{"image"} (default),
\code{"text"}, or \code{"both"}.}

\item{input_mode}{Character or \code{NULL}. Explicit input mode override. Default
\code{NULL}.}

\item{input_type}{Character. Type of input: \code{"auto"} (default), \code{"text"},
\code{"image"}, or \code{"pdf"}.}

\item{pdf_dpi}{Integer. DPI for PDF page rendering. Default \code{150L}.}

\item{creativity}{Numeric or \code{NULL}. Temperature setting. \code{NULL} uses the
provider default. Default \code{NULL}.}

\item{thinking_budget}{Integer. Extended thinking token budget (0 = off).
Default \code{0L}.}

\item{chain_of_thought}{Logical. Enable chain-of-thought reasoning. Default
\code{TRUE}.}

\item{context_prompt}{Logical. Add expert context to prompts. Default
\code{FALSE}.}

\item{step_back_prompt}{Logical. Enable step-back prompting. Default
\code{FALSE}.}

\item{filename}{Character or \code{NULL}. Output filename. Default \code{NULL}.}

\item{save_directory}{Character or \code{NULL}. Directory to save results.
Default \code{NULL}.}

\item{models}{A list of model specifications for multi-model ensemble mode.
Each element is either a 3-element character vector
\code{c("model", "provider", "api_key")} or a 4-element list
\code{list("model", "provider", "api_key", list(creativity = 0.5))}.
Default \code{NULL}.}

\item{max_workers}{Integer or \code{NULL}. Max parallel workers. \code{NULL} = auto.
Default \code{NULL}.}

\item{parallel}{Logical or \code{NULL}. Enable parallel processing. Default
\code{NULL}.}

\item{auto_download}{Logical. Auto-download Ollama models. Default \code{FALSE}.}

\item{safety}{Logical. If \code{TRUE}, saves progress after each item. Default
\code{FALSE}.}

\item{max_retries}{Integer. Max retries per API call. Default \code{5L}.}

\item{batch_retries}{Integer. Max retries for batch-level failures.
Default \code{1L}.}

\item{retry_delay}{Numeric. Seconds between retries. Default \code{1.0}.}

\item{row_delay}{Numeric. Seconds between processing each row. Default
\code{0.0}.}

\item{fail_strategy}{Character. How to handle failures: \code{"partial"}
(default) or \code{"strict"}.}

\item{batch_mode}{Logical. Use batch processing mode. Default \code{FALSE}.}

\item{batch_poll_interval}{Numeric. Seconds between batch status polls.
Default \code{30.0}.}

\item{batch_timeout}{Numeric. Maximum seconds to wait for batch completion.
Default \code{86400.0}.}

\item{auto_start_ollama}{Logical. If \code{TRUE} (default), automatically
call \code{\link[=ensure_ollama_running]{ensure_ollama_running()}} when \code{model_source = "ollama"} or
any ensemble entry uses the \code{"ollama"} provider. Set \code{FALSE} to
skip the check.}
}
\value{
A \code{data.frame} with summarization results.
}
\description{
Wraps the Python \code{cat_stack.summarize()} function. Generates summaries of
input data using one or more LLM models. Supports single-model and
multi-model (ensemble) summarization.
}
\examples{
\dontrun{
# Single-model summarization
results <- summarize(
  input_data   = c("A long article about climate change...",
                    "A detailed report on economic trends..."),
  description  = "News articles",
  instructions = "Provide a 2-sentence summary",
  api_key      = Sys.getenv("OPENAI_API_KEY")
)

# PDF summarization
results <- summarize(
  input_data = "path/to/documents/",
  input_type = "pdf",
  api_key    = Sys.getenv("OPENAI_API_KEY")
)
}

}
