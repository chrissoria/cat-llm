% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify}
\alias{classify}
\title{Classify survey responses using LLMs}
\usage{
classify(
  input_data,
  categories,
  survey_question = "",
  description = "",
  add_other = "prompt",
  check_verbosity = TRUE,
  api_key = NULL,
  user_model = "gpt-4o",
  mode = "image",
  creativity = NULL,
  safety = FALSE,
  chain_of_verification = FALSE,
  chain_of_thought = FALSE,
  step_back_prompt = FALSE,
  context_prompt = FALSE,
  thinking_budget = 0L,
  example1 = NULL,
  example2 = NULL,
  example3 = NULL,
  example4 = NULL,
  example5 = NULL,
  example6 = NULL,
  filename = NULL,
  save_directory = NULL,
  model_source = "auto",
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 10L,
  research_question = NULL,
  models = NULL,
  consensus_threshold = "unanimous",
  use_json_schema = TRUE,
  max_workers = NULL,
  fail_strategy = "partial",
  max_retries = 5L,
  batch_retries = 2L,
  retry_delay = 1,
  row_delay = 0,
  pdf_dpi = 150L,
  auto_download = FALSE,
  prompt_tune = NULL,
  tune_iterations = 1L,
  tune_ui = "browser",
  tune_optimize = "balanced"
)
}
\arguments{
\item{input_data}{A character vector, list, or \code{data.frame} column of survey
responses to classify.}

\item{categories}{A character vector of category names, or \code{"auto"} to
infer categories from the data.}

\item{survey_question}{Character. The survey question text. Default \code{""}.}

\item{description}{Character. Additional context for the classification task.
Default \code{""}.}

\item{add_other}{Logical or \code{"prompt"}. Controls addition of an "Other"
category. Default \code{"prompt"}.}

\item{check_verbosity}{Logical. Check category descriptions. Default \code{TRUE}.}

\item{api_key}{API key for the model provider (single-model mode).}

\item{user_model}{Character. Model name. Default \code{"gpt-4o"}.}

\item{mode}{Character. PDF processing mode. Default \code{"image"}.}

\item{creativity}{Numeric or \code{NULL}. Temperature. Default \code{NULL}.}

\item{safety}{Logical. Save progress after each item. Default \code{FALSE}.}

\item{chain_of_verification}{Logical. Default \code{FALSE}.}

\item{chain_of_thought}{Logical. Default \code{FALSE}.}

\item{step_back_prompt}{Logical. Default \code{FALSE}.}

\item{context_prompt}{Logical. Default \code{FALSE}.}

\item{thinking_budget}{Integer. Extended thinking budget. Default \code{0L}.}

\item{example1, example2, example3, example4, example5, example6}{Optional
few-shot examples.}

\item{filename}{Character or \code{NULL}. Output CSV filename.}

\item{save_directory}{Character or \code{NULL}. Output directory.}

\item{model_source}{Character. Provider hint. Default \code{"auto"}.}

\item{max_categories}{Integer. Max categories for auto mode. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Default \code{10L}.}

\item{divisions}{Integer. Default \code{10L}.}

\item{research_question}{Character or \code{NULL}. Optional research context.}

\item{models}{List of model specs for ensemble mode.}

\item{consensus_threshold}{Character or numeric. Default \code{"unanimous"}.}

\item{use_json_schema}{Logical. Default \code{TRUE}.}

\item{max_workers}{Integer or \code{NULL}. Default \code{NULL}.}

\item{fail_strategy}{Character. Default \code{"partial"}.}

\item{max_retries}{Integer. Default \code{5L}.}

\item{batch_retries}{Integer. Default \code{2L}.}

\item{retry_delay}{Numeric. Default \code{1.0}.}

\item{row_delay}{Numeric. Default \code{0.0}.}

\item{pdf_dpi}{Integer. Default \code{150L}.}

\item{auto_download}{Logical. Default \code{FALSE}.}

\item{prompt_tune}{Integer or \code{NULL}. Rows sampled per APO correction round. Default \code{NULL}.}

\item{tune_iterations}{Integer. APO optimization passes. Default \code{1L}.}

\item{tune_ui}{Character. Correction UI: \code{"browser"} or \code{"terminal"}. Default \code{"browser"}.}

\item{tune_optimize}{Character. Metric to optimize: \code{"balanced"}, \code{"sensitivity"}, or \code{"precision"}. Default \code{"balanced"}.}
}
\value{
A \code{data.frame} with classification results.
}
\description{
Wraps the Python \code{cat_survey.classify()} function. Adds \code{survey_question}
context to the base cat.stack classification engine.
}
\examples{
\dontrun{
results <- classify(
  input_data      = c("Took a new job in Chicago",
                      "Wanted to be closer to grandkids",
                      "Couldn't afford rent in the Bay Area"),
  categories      = c("Job/school", "Family", "Cost of living", "Other"),
  survey_question = "Why did you move?",
  api_key         = Sys.getenv("OPENAI_API_KEY"),
  user_model      = "gpt-4o-mini"
)
}
}
