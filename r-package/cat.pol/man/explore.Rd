% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{explore}
\alias{explore}
\title{Explore raw categories in political and policy documents}
\usage{
explore(
  input_data = NULL,
  api_key = NULL,
  source = NULL,
  doc_type = NULL,
  since = NULL,
  until = NULL,
  n = NULL,
  document_context = "",
  description = "",
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 12L,
  user_model = "gpt-4o",
  creativity = NULL,
  specificity = "broad",
  research_question = NULL,
  filename = NULL,
  model_source = "auto",
  iterations = 8L,
  random_state = NULL,
  focus = NULL,
  chunk_delay = 0
)
}
\arguments{
\item{input_data}{A character vector, list, or \code{NULL} to fetch from a
registered source. Default \code{NULL}.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{source}{Character or \code{NULL}. Registered source name.}

\item{doc_type}{Character or \code{NULL}. Filter source by document type.}

\item{since}{Character or \code{NULL}. Earliest source row date (YYYY-MM-DD).}

\item{until}{Character or \code{NULL}. Latest source row date (YYYY-MM-DD).}

\item{n}{Integer or \code{NULL}. Max number of source rows.}

\item{document_context}{Character. Context about the document. Default \code{""}.}

\item{description}{Character. Additional context. Default \code{""}.}

\item{max_categories}{Integer. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Default \code{10L}.}

\item{divisions}{Integer. Default \code{12L}.}

\item{user_model}{Character. Default \code{"gpt-4o"}.}

\item{creativity}{Numeric or \code{NULL}. Default \code{NULL}.}

\item{specificity}{Character. Default \code{"broad"}.}

\item{research_question}{Character or \code{NULL}.}

\item{filename}{Character or \code{NULL}.}

\item{model_source}{Character. Default \code{"auto"}.}

\item{iterations}{Integer. Default \code{8L}.}

\item{random_state}{Integer or \code{NULL}.}

\item{focus}{Character or \code{NULL}.}

\item{chunk_delay}{Numeric. Default \code{0.0}.}
}
\value{
A character vector of every category string extracted.
}
\description{
Wraps the Python \code{catpol.explore()} function. Returns every category
string extracted from every chunk across every iteration -- with
duplicates intact.
}
