% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify}
\alias{classify}
\title{Classify political and policy documents using LLMs}
\usage{
classify(
  categories,
  input_data = NULL,
  source = NULL,
  doc_type = NULL,
  since = NULL,
  until = NULL,
  n = NULL,
  document_context = "",
  description = "",
  api_key = NULL,
  user_model = "gpt-4o",
  mode = "image",
  creativity = NULL,
  safety = FALSE,
  chain_of_verification = FALSE,
  chain_of_thought = FALSE,
  step_back_prompt = FALSE,
  context_prompt = FALSE,
  thinking_budget = 0L,
  example1 = NULL,
  example2 = NULL,
  example3 = NULL,
  example4 = NULL,
  example5 = NULL,
  example6 = NULL,
  filename = NULL,
  save_directory = NULL,
  model_source = "auto",
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 10L,
  research_question = NULL,
  models = NULL,
  consensus_threshold = "unanimous",
  use_json_schema = TRUE,
  max_workers = NULL,
  fail_strategy = "partial",
  max_retries = 5L,
  batch_retries = 2L,
  retry_delay = 1,
  row_delay = 0,
  pdf_dpi = 150L,
  auto_download = FALSE,
  add_other = "prompt",
  check_verbosity = TRUE
)
}
\arguments{
\item{categories}{A character vector of category names.}

\item{input_data}{A character vector, list, or \code{data.frame} column, or
\code{NULL} to fetch from a registered source. Default \code{NULL}.}

\item{source}{Character or \code{NULL}. Registered source name (e.g.
\code{"city_san_diego"}, \code{"federal_laws"}, \code{"federal_executive_orders"},
\code{"social_trump_truth"}). Use \code{\link[=list_sources]{list_sources()}} for all options.}

\item{doc_type}{Character or \code{NULL}. Filter source by document type
(e.g. \code{"ordinance"}, \code{"resolution"}).}

\item{since}{Character or \code{NULL}. Earliest source row date (YYYY-MM-DD).}

\item{until}{Character or \code{NULL}. Latest source row date (YYYY-MM-DD).}

\item{n}{Integer or \code{NULL}. Max number of source rows to classify.}

\item{document_context}{Character. Context about the policy document
being analyzed. Default \code{""}.}

\item{description}{Character. Additional context description. Default \code{""}.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{user_model}{Character. Model name. Default \code{"gpt-4o"}.}

\item{mode}{Character. Processing mode. Default \code{"image"}.}

\item{creativity}{Numeric or \code{NULL}. Temperature. Default \code{NULL}.}

\item{safety}{Logical. Save progress after each item. Default \code{FALSE}.}

\item{chain_of_verification}{Logical. Default \code{FALSE}.}

\item{chain_of_thought}{Logical. Default \code{FALSE}.}

\item{step_back_prompt}{Logical. Default \code{FALSE}.}

\item{context_prompt}{Logical. Default \code{FALSE}.}

\item{thinking_budget}{Integer. Default \code{0L}.}

\item{example1, example2, example3, example4, example5, example6}{Optional
few-shot examples.}

\item{filename}{Character or \code{NULL}. Output CSV filename.}

\item{save_directory}{Character or \code{NULL}. Output directory.}

\item{model_source}{Character. Provider hint. Default \code{"auto"}.}

\item{max_categories}{Integer. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Default \code{10L}.}

\item{divisions}{Integer. Default \code{10L}.}

\item{research_question}{Character or \code{NULL}. Optional research context.}

\item{models}{List of model specs for ensemble mode. Default \code{NULL}.}

\item{consensus_threshold}{Character or numeric. Default \code{"unanimous"}.}

\item{use_json_schema}{Logical. Default \code{TRUE}.}

\item{max_workers}{Integer or \code{NULL}. Default \code{NULL}.}

\item{fail_strategy}{Character. Default \code{"partial"}.}

\item{max_retries}{Integer. Default \code{5L}.}

\item{batch_retries}{Integer. Default \code{2L}.}

\item{retry_delay}{Numeric. Default \code{1.0}.}

\item{row_delay}{Numeric. Default \code{0.0}.}

\item{pdf_dpi}{Integer. Default \code{150L}.}

\item{auto_download}{Logical. Default \code{FALSE}.}

\item{add_other}{Logical or \code{"prompt"}. Default \code{"prompt"}.}

\item{check_verbosity}{Logical. Default \code{TRUE}.}
}
\value{
A \code{data.frame} with classification results.
}
\description{
Wraps the Python \code{catpol.classify()} function. Can classify either raw
text (via \code{input_data}) or pull directly from a registered political
data source (via \code{source}). All catstack classification arguments are
supported.
}
\examples{
\dontrun{
# Pull recent San Diego ordinances from a registered source
results <- classify(
  source     = "city_san_diego",
  doc_type   = "ordinance",
  since      = "2024-01-01",
  n          = 50L,
  categories = c("Housing", "Public Safety", "Finance",
                 "Infrastructure", "Health"),
  api_key    = Sys.getenv("OPENAI_API_KEY"),
  user_model = "gpt-4o-mini"
)

# Or classify your own text directly
results <- classify(
  input_data = df$bill_text,
  categories = c("Housing", "Public Safety", "Finance"),
  api_key    = Sys.getenv("OPENAI_API_KEY")
)
}
}
