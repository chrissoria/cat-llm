% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize}
\alias{summarize}
\title{Summarize political and policy documents using LLMs}
\usage{
summarize(
  input_data = NULL,
  source = NULL,
  doc_type = NULL,
  since = NULL,
  until = NULL,
  n = NULL,
  format = "paragraph",
  tone = "eli5",
  api_key = NULL,
  description = "",
  instructions = "",
  max_length = NULL,
  focus = NULL,
  user_model = "gpt-4o",
  model_source = "auto",
  mode = "image",
  input_mode = NULL,
  input_type = "auto",
  pdf_dpi = 150L,
  creativity = NULL,
  thinking_budget = 0L,
  chain_of_thought = TRUE,
  context_prompt = FALSE,
  step_back_prompt = FALSE,
  filename = NULL,
  save_directory = NULL,
  models = NULL,
  max_workers = NULL,
  parallel = NULL,
  auto_download = FALSE,
  safety = FALSE,
  max_retries = 5L,
  batch_retries = 2L,
  retry_delay = 1,
  row_delay = 0,
  fail_strategy = "partial",
  batch_mode = FALSE,
  batch_poll_interval = 30,
  batch_timeout = 86400
)
}
\arguments{
\item{input_data}{A character vector, list, or PDF/URL paths; \code{NULL} to
fetch from a registered source.}

\item{source}{Character or \code{NULL}. Registered source name.}

\item{doc_type}{Character or \code{NULL}. Filter source by document type.}

\item{since}{Character or \code{NULL}. Earliest source row date (YYYY-MM-DD).}

\item{until}{Character or \code{NULL}. Latest source row date (YYYY-MM-DD).}

\item{n}{Integer or \code{NULL}. Max number of source rows.}

\item{format}{Character. Output format. Default \code{"paragraph"}.}

\item{tone}{Character. Policy-specific tone, e.g. \code{"eli5"}, \code{"neutral"},
\code{"academic"}. Default \code{"eli5"}.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{description}{Character. Default \code{""}.}

\item{instructions}{Character. Specific instructions for the summary.
Default \code{""}.}

\item{max_length}{Integer or \code{NULL}. Default \code{NULL}.}

\item{focus}{Character or \code{NULL}. Default \code{NULL}.}

\item{user_model}{Character. Default \code{"gpt-4o"}.}

\item{model_source}{Character. Default \code{"auto"}.}

\item{mode}{Character. Default \code{"image"}.}

\item{input_mode}{Character or \code{NULL}. Default \code{NULL}.}

\item{input_type}{Character. Default \code{"auto"}.}

\item{pdf_dpi}{Integer. Default \code{150L}.}

\item{creativity}{Numeric or \code{NULL}. Default \code{NULL}.}

\item{thinking_budget}{Integer. Default \code{0L}.}

\item{chain_of_thought}{Logical. Default \code{TRUE}.}

\item{context_prompt}{Logical. Default \code{FALSE}.}

\item{step_back_prompt}{Logical. Default \code{FALSE}.}

\item{filename}{Character or \code{NULL}.}

\item{save_directory}{Character or \code{NULL}.}

\item{models}{List of model specs for ensemble mode. Default \code{NULL}.}

\item{max_workers}{Integer or \code{NULL}. Default \code{NULL}.}

\item{parallel}{Logical or \code{NULL}. Default \code{NULL}.}

\item{auto_download}{Logical. Default \code{FALSE}.}

\item{safety}{Logical. Default \code{FALSE}.}

\item{max_retries}{Integer. Default \code{5L}.}

\item{batch_retries}{Integer. Default \code{2L}.}

\item{retry_delay}{Numeric. Default \code{1.0}.}

\item{row_delay}{Numeric. Default \code{0.0}.}

\item{fail_strategy}{Character. Default \code{"partial"}.}

\item{batch_mode}{Logical. Default \code{FALSE}.}

\item{batch_poll_interval}{Numeric. Default \code{30.0}.}

\item{batch_timeout}{Numeric. Default \code{86400.0}.}
}
\value{
A \code{data.frame} with summarization results.
}
\description{
Wraps the Python \code{catpol.summarize()} function. Generates summaries from
policy text or from a registered political data source. Adds a \code{tone}
parameter for policy-specific framing.
}
