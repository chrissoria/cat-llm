% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract}
\alias{extract}
\title{Extract categories from social media content using LLMs}
\usage{
extract(
  input_data = NULL,
  api_key = NULL,
  sm_source = NULL,
  sm_limit = 50L,
  sm_months = NULL,
  sm_credentials = NULL,
  platform = NULL,
  handle = NULL,
  hashtags = NULL,
  post_metadata = NULL,
  description = "",
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 12L,
  user_model = "gpt-4o",
  creativity = NULL,
  specificity = "broad",
  research_question = NULL,
  mode = "text",
  filename = NULL,
  model_source = "auto",
  iterations = 8L,
  random_state = NULL,
  focus = NULL,
  chunk_delay = 0
)
}
\arguments{
\item{input_data}{A character vector, list, or \code{NULL} to fetch from social
media. Default \code{NULL}.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{sm_source}{Character or \code{NULL}. Social media source.}

\item{sm_limit}{Integer. Max posts to fetch. Default \code{50L}.}

\item{sm_months}{Integer or \code{NULL}. Fetch posts from last N months.}

\item{sm_credentials}{Named list or \code{NULL}. API credentials.}

\item{platform}{Character or \code{NULL}. Alias for \code{sm_source}.}

\item{handle}{Character or \code{NULL}. Social media handle.}

\item{hashtags}{Character vector or \code{NULL}. Hashtags to filter by.}

\item{post_metadata}{Named list or \code{NULL}. Additional post metadata.}

\item{description}{Character. Context description. Default \code{""}.}

\item{max_categories}{Integer. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Default \code{10L}.}

\item{divisions}{Integer. Default \code{12L}.}

\item{user_model}{Character. Default \code{"gpt-4o"}.}

\item{creativity}{Numeric or \code{NULL}. Default \code{NULL}.}

\item{specificity}{Character. Default \code{"broad"}.}

\item{research_question}{Character or \code{NULL}.}

\item{mode}{Character. Default \code{"text"}.}

\item{filename}{Character or \code{NULL}.}

\item{model_source}{Character. Default \code{"auto"}.}

\item{iterations}{Integer. Default \code{8L}.}

\item{random_state}{Integer or \code{NULL}.}

\item{focus}{Character or \code{NULL}.}

\item{chunk_delay}{Numeric. Default \code{0.0}.}
}
\value{
A named list with \code{counts_df}, \code{top_categories}, and \code{raw_top_text}.
}
\description{
Wraps the Python \code{catvader.extract()} function. Discovers and returns a
normalised, deduplicated set of categories from social media data.
}
\examples{
\dontrun{
result <- extract(
  input_data = df$posts,
  api_key    = Sys.getenv("OPENAI_API_KEY"),
  user_model = "gpt-4o-mini"
)
print(result$top_categories)
}
}
