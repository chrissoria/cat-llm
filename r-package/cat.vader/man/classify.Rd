% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify}
\alias{classify}
\title{Classify social media content using LLMs}
\usage{
classify(
  input_data = NULL,
  categories = NULL,
  api_key = NULL,
  sm_source = NULL,
  sm_limit = 50L,
  sm_months = NULL,
  sm_days = NULL,
  sm_credentials = NULL,
  sm_handle = NULL,
  sm_timezone = "UTC",
  sm_youtube_content = "video",
  sm_youtube_transcript = FALSE,
  sm_comments_per_video = 20L,
  sm_youtube_transcript_max_chars = 10000L,
  platform = NULL,
  handle = NULL,
  hashtags = NULL,
  post_metadata = NULL,
  description = "",
  feed_question = "",
  user_model = "gpt-4o",
  mode = "image",
  creativity = NULL,
  safety = FALSE,
  chain_of_verification = FALSE,
  chain_of_thought = FALSE,
  step_back_prompt = FALSE,
  context_prompt = FALSE,
  thinking_budget = 0L,
  example1 = NULL,
  example2 = NULL,
  example3 = NULL,
  example4 = NULL,
  example5 = NULL,
  example6 = NULL,
  filename = NULL,
  save_directory = NULL,
  model_source = "auto",
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 10L,
  research_question = NULL,
  models = NULL,
  consensus_threshold = "unanimous",
  use_json_schema = TRUE,
  max_workers = NULL,
  fail_strategy = "partial",
  max_retries = 5L,
  batch_retries = 2L,
  retry_delay = 1,
  row_delay = 0,
  pdf_dpi = 150L,
  auto_download = FALSE,
  add_other = "prompt",
  check_verbosity = TRUE,
  prompt_tune = NULL,
  tune_iterations = 1L,
  tune_ui = "browser",
  tune_optimize = "balanced"
)
}
\arguments{
\item{input_data}{A character vector, list, or \code{data.frame} column, or
\code{NULL} to fetch from social media. Default \code{NULL}.}

\item{categories}{A character vector of category names, or \code{"auto"}.
Default \code{NULL}.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{sm_source}{Character or \code{NULL}. Social media source (e.g.,
\code{"reddit"}, \code{"twitter"}, \code{"youtube"}).}

\item{sm_limit}{Integer. Max posts to fetch. Default \code{50L}.}

\item{sm_months}{Integer or \code{NULL}. Fetch posts from last N months.}

\item{sm_days}{Integer or \code{NULL}. Fetch posts from last N days.}

\item{sm_credentials}{Named list or \code{NULL}. API credentials for the
social media platform.}

\item{sm_handle}{Character or \code{NULL}. Social media handle to fetch from.}

\item{sm_timezone}{Character. Timezone for date filtering. Default \code{"UTC"}.}

\item{sm_youtube_content}{Character. YouTube content type. Default \code{"video"}.}

\item{sm_youtube_transcript}{Logical. Include YouTube transcripts. Default
\code{FALSE}.}

\item{sm_comments_per_video}{Integer. Comments per YouTube video. Default
\code{20L}.}

\item{sm_youtube_transcript_max_chars}{Integer. Max transcript chars. Default
\code{10000L}.}

\item{platform}{Character or \code{NULL}. Alias for \code{sm_source}.}

\item{handle}{Character or \code{NULL}. Alias for \code{sm_handle}.}

\item{hashtags}{Character vector or \code{NULL}. Hashtags to filter by.}

\item{post_metadata}{Named list or \code{NULL}. Additional post metadata.}

\item{description}{Character. Context description. Default \code{""}.}

\item{feed_question}{Character. Feed-specific question context. Default \code{""}.}

\item{user_model}{Character. Model name. Default \code{"gpt-4o"}.}

\item{mode}{Character. Processing mode. Default \code{"image"}.}

\item{creativity}{Numeric or \code{NULL}. Temperature. Default \code{NULL}.}

\item{safety}{Logical. Save progress after each item. Default \code{FALSE}.}

\item{chain_of_verification}{Logical. Default \code{FALSE}.}

\item{chain_of_thought}{Logical. Default \code{FALSE}.}

\item{step_back_prompt}{Logical. Default \code{FALSE}.}

\item{context_prompt}{Logical. Default \code{FALSE}.}

\item{thinking_budget}{Integer. Default \code{0L}.}

\item{example1, example2, example3, example4, example5, example6}{Optional
few-shot examples.}

\item{filename}{Character or \code{NULL}. Output CSV filename.}

\item{save_directory}{Character or \code{NULL}. Output directory.}

\item{model_source}{Character. Provider hint. Default \code{"auto"}.}

\item{max_categories}{Integer. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Default \code{10L}.}

\item{divisions}{Integer. Default \code{10L}.}

\item{research_question}{Character or \code{NULL}. Optional research context.}

\item{models}{List of model specs for ensemble mode.}

\item{consensus_threshold}{Character or numeric. Default \code{"unanimous"}.}

\item{use_json_schema}{Logical. Default \code{TRUE}.}

\item{max_workers}{Integer or \code{NULL}. Default \code{NULL}.}

\item{fail_strategy}{Character. Default \code{"partial"}.}

\item{max_retries}{Integer. Default \code{5L}.}

\item{batch_retries}{Integer. Default \code{2L}.}

\item{retry_delay}{Numeric. Default \code{1.0}.}

\item{row_delay}{Numeric. Default \code{0.0}.}

\item{pdf_dpi}{Integer. Default \code{150L}.}

\item{auto_download}{Logical. Default \code{FALSE}.}

\item{add_other}{Logical or \code{"prompt"}. Default \code{"prompt"}.}

\item{check_verbosity}{Logical. Default \code{TRUE}.}
}
\value{
A \code{data.frame} with classification results.
}
\description{
Wraps the Python \code{catvader.classify()} function. Adds social media sourcing
parameters to the base cat.stack classification engine.
}
\examples{
\dontrun{
# Classify your own text directly
results <- classify(
  input_data = c("Just had the best coffee ever!",
                 "Politicians are all the same",
                 "Looking forward to the game tonight"),
  categories = c("Positive", "Negative", "Neutral"),
  api_key    = Sys.getenv("OPENAI_API_KEY"),
  user_model = "gpt-4o-mini"
)
}
}
