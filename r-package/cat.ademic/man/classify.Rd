% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify}
\alias{classify}
\title{Classify academic papers using LLMs}
\usage{
classify(
  categories,
  input_data = NULL,
  api_key = NULL,
  journal_issn = NULL,
  journal_name = NULL,
  journal_field = NULL,
  topic_name = NULL,
  topic_id = NULL,
  paper_limit = 50L,
  date_from = NULL,
  date_to = NULL,
  polite_email = NULL,
  journal = NULL,
  field = NULL,
  research_focus = NULL,
  paper_metadata = NULL,
  description = "",
  filename = NULL,
  save_directory = NULL,
  user_model = "gpt-4o",
  mode = "image",
  creativity = NULL,
  safety = FALSE,
  chain_of_verification = FALSE,
  chain_of_thought = FALSE,
  step_back_prompt = FALSE,
  context_prompt = FALSE,
  thinking_budget = 0L,
  example1 = NULL,
  example2 = NULL,
  example3 = NULL,
  example4 = NULL,
  example5 = NULL,
  example6 = NULL,
  model_source = "auto",
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 10L,
  research_question = NULL,
  models = NULL,
  consensus_threshold = "unanimous",
  use_json_schema = TRUE,
  max_workers = NULL,
  fail_strategy = "partial",
  max_retries = 5L,
  batch_retries = 2L,
  retry_delay = 1,
  row_delay = 0,
  pdf_dpi = 150L,
  auto_download = FALSE,
  add_other = "prompt",
  check_verbosity = TRUE,
  prompt_tune = NULL,
  tune_iterations = 1L,
  tune_ui = "browser",
  tune_optimize = "balanced"
)
}
\arguments{
\item{categories}{A character vector of category names, or \code{"auto"}.}

\item{input_data}{A character vector, list, or \code{data.frame} column, or
\code{NULL} to fetch from academic sources. Default \code{NULL}.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{journal_issn}{Character or \code{NULL}. Journal ISSN to fetch papers from.}

\item{journal_name}{Character or \code{NULL}. Journal name to fetch papers from.}

\item{journal_field}{Character or \code{NULL}. Academic field to filter by.}

\item{topic_name}{Character or \code{NULL}. Topic name to search for.}

\item{topic_id}{Character or \code{NULL}. OpenAlex topic ID.}

\item{paper_limit}{Integer. Max papers to fetch. Default \code{50L}.}

\item{date_from}{Character or \code{NULL}. Start date (YYYY-MM-DD).}

\item{date_to}{Character or \code{NULL}. End date (YYYY-MM-DD).}

\item{polite_email}{Character or \code{NULL}. Email for polite API pool.}

\item{journal}{Character or \code{NULL}. Alias for \code{journal_name}.}

\item{field}{Character or \code{NULL}. Alias for \code{journal_field}.}

\item{research_focus}{Character or \code{NULL}. Research focus filter.}

\item{paper_metadata}{Named list or \code{NULL}. Additional paper metadata.}

\item{description}{Character. Context description. Default \code{""}.}

\item{filename}{Character or \code{NULL}. Output CSV filename.}

\item{save_directory}{Character or \code{NULL}. Output directory.}

\item{user_model}{Character. Model name. Default \code{"gpt-4o"}.}

\item{mode}{Character. Processing mode. Default \code{"image"}.}

\item{creativity}{Numeric or \code{NULL}. Temperature. Default \code{NULL}.}

\item{safety}{Logical. Save progress after each item. Default \code{FALSE}.}

\item{chain_of_verification}{Logical. Default \code{FALSE}.}

\item{chain_of_thought}{Logical. Default \code{FALSE}.}

\item{step_back_prompt}{Logical. Default \code{FALSE}.}

\item{context_prompt}{Logical. Default \code{FALSE}.}

\item{thinking_budget}{Integer. Default \code{0L}.}

\item{example1, example2, example3, example4, example5, example6}{Optional
few-shot examples.}

\item{model_source}{Character. Provider hint. Default \code{"auto"}.}

\item{max_categories}{Integer. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Default \code{10L}.}

\item{divisions}{Integer. Default \code{10L}.}

\item{research_question}{Character or \code{NULL}. Optional research context.}

\item{models}{List of model specs for ensemble mode.}

\item{consensus_threshold}{Character or numeric. Default \code{"unanimous"}.}

\item{use_json_schema}{Logical. Default \code{TRUE}.}

\item{max_workers}{Integer or \code{NULL}. Default \code{NULL}.}

\item{fail_strategy}{Character. Default \code{"partial"}.}

\item{max_retries}{Integer. Default \code{5L}.}

\item{batch_retries}{Integer. Default \code{2L}.}

\item{retry_delay}{Numeric. Default \code{1.0}.}

\item{row_delay}{Numeric. Default \code{0.0}.}

\item{pdf_dpi}{Integer. Default \code{150L}.}

\item{auto_download}{Logical. Default \code{FALSE}.}

\item{add_other}{Logical or \code{"prompt"}. Default \code{"prompt"}.}

\item{check_verbosity}{Logical. Default \code{TRUE}.}

\item{prompt_tune}{Integer or \code{NULL}. Rows sampled per APO correction round. Default \code{NULL}.}

\item{tune_iterations}{Integer. APO optimization passes. Default \code{1L}.}

\item{tune_ui}{Character. Correction UI: \code{"browser"} or \code{"terminal"}. Default \code{"browser"}.}

\item{tune_optimize}{Character. Metric to optimize: \code{"balanced"}, \code{"sensitivity"}, or \code{"precision"}. Default \code{"balanced"}.}
}
\value{
A \code{data.frame} with classification results.
}
\description{
Wraps the Python \code{catademic.classify()} function. Adds journal and topic
sourcing parameters to the base cat.stack classification engine.
}
\examples{
\dontrun{
# Classify abstracts directly
results <- classify(
  categories = c("Methods", "Theory", "Review", "Other"),
  input_data = df$abstract,
  mode       = "text",
  api_key    = Sys.getenv("OPENAI_API_KEY"),
  user_model = "gpt-4o-mini"
)

# Fetch papers from a journal via OpenAlex
results <- classify(
  categories   = c("Empirical", "Theoretical", "Review"),
  journal_name = "American Sociological Review",
  paper_limit  = 100L,
  polite_email = "you@university.edu",
  api_key      = Sys.getenv("OPENAI_API_KEY")
)
}
}
