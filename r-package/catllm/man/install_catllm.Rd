% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_catllm}
\alias{install_catllm}
\title{Install the catllm Python package}
\usage{
install_catllm(
  method = "auto",
  conda = "auto",
  pdf = FALSE,
  upgrade = FALSE,
  ...
)
}
\arguments{
\item{method}{Installation method: \code{"auto"} (default), \code{"pip"}, or
\code{"conda"}.}

\item{conda}{Name of the conda environment to install into (only relevant
when \code{method = "conda"}).}

\item{pdf}{Logical. If \code{TRUE}, also installs the optional PDF-processing
dependencies (\code{catllm[pdf]}). Default \code{FALSE}.}

\item{upgrade}{Logical. If \code{TRUE}, upgrades an existing \code{catllm}
installation to the latest version. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[reticulate:py_install]{reticulate::py_install()}}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect.
}
\description{
A convenience wrapper around \code{\link[reticulate:py_install]{reticulate::py_install()}} that installs the
\code{catllm} Python package (and optional PDF-support extras) into the active
Python environment.
}
\examples{
\dontrun{
# Basic install
install_catllm()

# Install with PDF support
install_catllm(pdf = TRUE)

# Upgrade to latest
install_catllm(upgrade = TRUE)
}

}
