% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{explore}
\alias{explore}
\title{Explore raw categories in text data}
\usage{
explore(
  input_data,
  api_key,
  description = "",
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 12L,
  user_model = "gpt-4o",
  creativity = NULL,
  specificity = "broad",
  research_question = NULL,
  filename = NULL,
  model_source = "auto",
  iterations = 8L,
  random_state = NULL,
  focus = NULL,
  chunk_delay = 0
)
}
\arguments{
\item{input_data}{A character vector, list, or \code{data.frame} column of text
responses.}

\item{api_key}{Character. API key for the model provider.}

\item{description}{Character. The survey question or data description.
Default \code{""}.}

\item{max_categories}{Integer. Maximum categories per chunk. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Categories to extract per chunk.
Default \code{10L}.}

\item{divisions}{Integer. Number of data chunks. Default \code{12L}.}

\item{user_model}{Character. Model name. Default \code{"gpt-4o"}.}

\item{creativity}{Numeric or \code{NULL}. Temperature setting. \code{NULL} uses the
provider default. Default \code{NULL}.}

\item{specificity}{Character. \code{"broad"} (default) or \code{"specific"}.}

\item{research_question}{Character or \code{NULL}. Optional research context.}

\item{filename}{Character or \code{NULL}. Optional CSV filename to save the raw
category list.}

\item{model_source}{Character. Provider hint. Default \code{"auto"}.}

\item{iterations}{Integer. Number of passes over the data. Default \code{8L}.}

\item{random_state}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{focus}{Character or \code{NULL}. Optional focus instruction.}

\item{chunk_delay}{Numeric. Seconds between API calls. Default \code{0.0}.}
}
\value{
A character vector of every category string extracted across all
chunks and iterations. Length is approximately
\verb{iterations × divisions × categories_per_chunk}.
}
\description{
Wraps the Python \code{catllm.explore()} function. Returns every category string
extracted from every chunk across every iteration — with duplicates intact.
Useful for analysing category stability and saturation across repeated
extraction runs.
}
\details{
Unlike \code{\link[=extract]{extract()}}, which normalises and deduplicates categories, \code{explore()}
returns the raw unprocessed output suitable for frequency and saturation
analysis.
}
\examples{
\dontrun{
raw_cats <- explore(
  input_data  = df$responses,
  description = "Why did you move?",
  api_key     = Sys.getenv("OPENAI_API_KEY"),
  iterations  = 3L,
  divisions   = 5L
)
length(raw_cats)   # ~150
head(raw_cats, 10)
}

}
