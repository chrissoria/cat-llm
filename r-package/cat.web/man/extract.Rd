% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract}
\alias{extract}
\title{Discover categories from web content using LLMs}
\usage{
extract(
  input_data = NULL,
  api_key = NULL,
  source_domain = NULL,
  content_type = NULL,
  web_metadata = NULL,
  description = "",
  timeout = 30L,
  max_categories = 12L,
  categories_per_chunk = 10L,
  divisions = 12L,
  user_model = "gpt-4o",
  creativity = NULL,
  specificity = "broad",
  research_question = NULL,
  mode = "text",
  filename = NULL,
  model_source = "auto",
  iterations = 8L,
  random_state = NULL,
  focus = NULL,
  chunk_delay = 0
)
}
\arguments{
\item{input_data}{A character vector / list of URLs or text. Default \code{NULL}.}

\item{api_key}{Character or \code{NULL}. API key for the LLM provider.}

\item{source_domain}{Character or \code{NULL}. Source domain context.}

\item{content_type}{Character or \code{NULL}. Content type context.}

\item{web_metadata}{Named list or \code{NULL}. Additional metadata.}

\item{description}{Character. Default \code{""}.}

\item{timeout}{Integer. URL fetch timeout (seconds). Default \code{30L}.}

\item{max_categories}{Integer. Default \code{12L}.}

\item{categories_per_chunk}{Integer. Default \code{10L}.}

\item{divisions}{Integer. Default \code{12L}.}

\item{user_model}{Character. Default \code{"gpt-4o"}.}

\item{creativity}{Numeric or \code{NULL}. Default \code{NULL}.}

\item{specificity}{Character. Default \code{"broad"}.}

\item{research_question}{Character or \code{NULL}.}

\item{mode}{Character. Default \code{"text"}.}

\item{filename}{Character or \code{NULL}.}

\item{model_source}{Character. Default \code{"auto"}.}

\item{iterations}{Integer. Default \code{8L}.}

\item{random_state}{Integer or \code{NULL}.}

\item{focus}{Character or \code{NULL}.}

\item{chunk_delay}{Numeric. Default \code{0.0}.}
}
\value{
A named list with \code{counts_df}, \code{top_categories}, and \code{raw_top_text}.
}
\description{
Wraps the Python \code{catweb.extract()} function. Accepts URLs (auto-fetched)
or raw text. Returns a normalised, deduplicated set of categories.
}
\examples{
\dontrun{
result <- extract(
  input_data    = c("https://example.com/page1",
                    "https://example.com/page2"),
  source_domain = "example.com",
  api_key       = Sys.getenv("OPENAI_API_KEY"),
  user_model    = "gpt-4o-mini"
)
print(result$top_categories)
}
}
